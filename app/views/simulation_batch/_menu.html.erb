<div class="add-filter">
	<p><label><%=l(:label_actions)%>: </label><%= select_tag 'simulation_batch_action',
	 options_for_select(simulation_batch_select_options()), 	:onchange=> "doAction(this.options[this.selectedIndex].value);"  

	%></p>
</div>

<script>
function doAction(val)
{
	if(val == 'generate'){
		show_box("report-generator");
	}
}
</script>